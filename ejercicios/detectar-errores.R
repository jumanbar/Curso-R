## Para la semana I

## Detectar el error:

cos()                   # Falta un argumento

u <- plot(1:5 ^ 5)      # No hay error

x -> exp(1)             # Asignación en la dirección incorrecta

6 + 1,3                 # Sobra una coma

y <- x ** 4 + 5         # No hay error

x <- 2 + 3)             # Falta un paréntesis

x <- (exp(4 - sin(- pi)) (3 / (6 * cos(- (1 / 3))))) # Falta un operador

x <- 3 2                # Falta un operador o sobra un espacio

y <- 8:-3               # No hay error

log(8 2)                # Falta una coma

round(exp(1). 3)        # Hay un punto donde debe haber una coma

h <- exp(v <- 5)        # No hay error

exists(x)               # Faltan comillas

miobjeto <- {           # Se invoca un objeto no existente
  x <- matrix(0, 3, 4)
  diag(x) <- 1
  y <- x + 3
  y <- y + z
}

x <- matrix(1:9, 3, 3)  # No hay error
colnames(x) <- c('es', 'un', 'ejemplo')
