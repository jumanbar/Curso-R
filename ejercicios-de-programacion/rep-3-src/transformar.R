# Rep. III - ej. 1.e

# Instrucciones: 
# Utilizar la función apply y la función est para transformar (normalizar) 
# los datos de la data.frame usa3. No es necesario haber completado el 
# ejercicio 1.d (función est) para hacer esta parte, ya que si puede
# cargar una función est para el ejercicio con el comando load('est.RData')

# Objetivos:

# datosNumericos: matriz o data.frame con las clumnas de usa3 de clase 
#                 numeric. Nótese que si usted ha completado los ejercicios 
#                 anteriores, no todas las variables de dicho objeto son de 
#                 esta clase.
# datosTrans*: variables numéricas de usa3 transformadas con la función est. 
#              Este objeto, como se indica abajo, *debe ser creado con la función 
#              apply*. Es importante notar que debido al modo de funcionamiento 
#              de apply, datosTrans deberá ser un objeto de clase "matrix".
# usaNorm*: data.frame basada en el usa3, cuyas variables numéricas están 
#           estandarizadas por la función est. Nótese que debe ser un objeto de 
#           clase data.frame, debe contener los mismos nombres (y en el mismo 
#           orden) de filas y columnas que el objeto inicial, así como las variables 
#           no numéricas.

#===== Su código comienza aquí: =====#

datosNumericos <- 0 # (matrix o data.frame)

datosTrans <- 0 # (matrix) Aquí debe usarse apply con datosNumericos como primer argumento

usaNorm <- 0 # (data.frame)

#====== Aquí finaliza su código =====#
