# Instrucciones: deben importarse los datos del archivo 'usa-extra.csv' 
# mediante el comando correcto. Posteriormente se deben usar los comandos 
# necesarios para sustituir los NAs en la columna "Analf" de usa2 por los 
# datos correspondientes de usa-extra.csv. Nótese que usa-extra no tiene las 
# mismas filas ni columnas que usa.xls y por lo tanto esto afecta la ubicación 
# de los datos correctos a utilizar.
# usa2*: data.frame con los mismos datos que 'usa', excepto por los valores NA 
#   en la columna 'Analf', en la que deben estar los datos correctos, tomados 
#   de usa-extra.csv.

# Nota: el archivo usa-extra.csv se actualiza cada vez que se ejecuta 
#   'evaluar()', usando valores aleatorios.
#===== Su código comienza aquí: =====#

usa2 <- usa # Para mantener la data.frame usa incambiada...

#====== Aquí finaliza su código =====#

